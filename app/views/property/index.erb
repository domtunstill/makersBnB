<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1> <%= @property.name %> </h1>
    <p> <%= @property.description %> </p>
    <p> <%= @property.price_per_night %> </p>
    <p>
      <%d1 = @property.available_from %>
      Available from: <%= d1.strftime("%A #{d1.day.ordinalize} %B %Y")
      %>
    </p>
    <p>
      <%d2 = @property.available_to %>
      Available to: <%= d2.strftime("%A #{d2.day.ordinalize} %B %Y")
      %>
    </p>

    <a class="btn btn-primary my-2 my-sm-0"
        href="/property/<%= @property.id %>/booking/new"
        role="button" name="book"
        >
        Book</a>
  </body>
</html>
